/// Para mantener centralizadas constantes
///
/// Por ejemplo las llaves que se guardan en localstorage
class StorageKeys {
  static const String tokenKey = 'token';
  static const String themeKey = 'theme';
}
